apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: photon-system
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: "" 
  volumeName: data-pv